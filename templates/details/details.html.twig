{% extends 'base.html.twig' %}

{% block main %}
<div class="container mx-auto p-4">
    <h1>Détails de la Dette</h1>
    <p><strong>Client:</strong> {{ dette.client.surname }}</p>
    <p><strong>Montant:</strong> {{ dette.montant }}</p>
    <p><strong>Montant Versé:</strong> {{ dette.montantVerse }}</p>
    <p><strong>Date:</strong> {{ dette.dateAt|date('d/m/Y') }}</p>
    <a href="{{ path('dette.index') }}" class="bg-gray-500 text-white py-2 px-4 rounded">Retour</a>
</div>
{% endblock %}
